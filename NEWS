v0.1.1  (2024-01-31)

  o read_dataset: 'detect.dates' ignores empty rows in
    date columns, whose value becomes NA

v0.1.0  (2023-09-22)

  o Initial release. The package provides functions for
    downloading, reading and comparing datasets from the Swiss
    National Science Foundation (SNF, FNS, SNSF; https://snf.ch).
    Downloaded data can optionally be cached, to avoid repeated
    downloads of the same files.  The package is lightweight and
    without dependencies.
